import React from "react";

// const List = props => (
        
        // componentDidMount() {
        //     this.loadBooks();
        //   }
        
        //   loadBooks = () => {
        //     API.getBooks()
        //       .then(res => this.setState({ books: res.data }))
        //       .catch(err => console.log(err));
        //   };
    
// )

export default List;